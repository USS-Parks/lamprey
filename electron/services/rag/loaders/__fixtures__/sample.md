# Sample Markdown

This is a small fixture used by `loaders.test.ts` to verify the text
loader's UTF-8 round trip and mime detection.

## Section A

Body text under section A. Padding so the file is comfortably above the
loader's binary-sniff threshold and the chunker's MIN_CHUNK_CHARS floor.
