name: git-show
description: Strip ANSI + cap line length on git show
match:
  command: git
  subcommand: show
  excludeFlags:
    - '--stat'
    - '--name-only'
    - '--name-status'
pipeline:
  - action: strip_ansi
  - action: truncate_lines
    max: 200
  - action: head
    n: 400
