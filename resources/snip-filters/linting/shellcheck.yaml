name: shellcheck
description: Compress shellcheck output to issue list
match:
  command: shellcheck
pipeline:
  - action: strip_ansi
  - action: keep_lines
    pattern: '(In .* line \d+|SC\d+|warning|error|note)'
  - action: head
    n: 100
  - action: on_empty
    message: 'shellcheck: clean'
