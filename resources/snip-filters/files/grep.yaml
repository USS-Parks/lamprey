name: grep
description: Bounded grep output (head 100 + per-line cap)
match:
  command: grep
pipeline:
  - action: strip_ansi
  - action: truncate_lines
    max: 300
  - action: head
    n: 100
  - action: on_empty
    message: 'grep: no matches'
