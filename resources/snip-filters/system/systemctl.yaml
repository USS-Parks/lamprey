name: systemctl
description: Bound systemctl status / list output
match:
  command: systemctl
pipeline:
  - action: strip_ansi
  - action: head
    n: 80
  - action: on_empty
    message: 'systemctl: no output'
