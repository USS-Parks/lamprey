export function PluginsColumn() {
  // C9 replaces this with the live plugin list (groups by category).
  return (
    <div className="flex h-full items-center justify-center p-6 text-center text-[12px] text-[var(--text-muted)]">
      Plugins list lands in C9.
    </div>
  )
}
