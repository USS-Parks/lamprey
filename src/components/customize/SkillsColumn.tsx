export function SkillsColumn() {
  // C2 replaces this with the live skill list + per-row affordances.
  return (
    <div className="flex h-full items-center justify-center p-6 text-center text-[12px] text-[var(--text-muted)]">
      Skills list lands in C2.
    </div>
  )
}
